Select the best answer

1. A/Column Arranges child items into a column by stacking them on top of each other
B/ Column Places its child items next to each other

2. A/ In Grid, If rows or columns set, the other is calculated from the number of child items
B/ Grid rows property: number of rows can not be constrained
C/ Grid columns property: number of columns can be constrained

3. A/ The last child element with focus:false receives the focus if the FocusScope receives the focus
B/ The last child element with focus:true receives the focus if the FocusScope receives the focus

4. A/ Direction of the flow is controlled using flow and layoutDirection
B/ Flow dont need both width and height

